{
  "name": "wicked-crew-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/crew",
    "packages/studio"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
