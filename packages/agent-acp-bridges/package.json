{
  "name": "agent-acp-bridges",
  "version": "1.1.1",
  "description": "ACP (Agent Client Protocol) stdio bridge for headless coding CLIs without a native or ecosystem ACP adapter — currently agy (Antigravity) — plus the `wicked-pi` launcher that hands a pi seat its skills through the community pi-acp adapter. Used by wicked-core to drive governed multi-turn sessions.",
  "type": "module",
  "bin": {
    "agy-acp": "agy-acp.mjs",
    "wicked-pi": "wicked-pi.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikeparcewski/wicked-crew.git",
    "directory": "packages/agent-acp-bridges"
  },
  "keywords": [
    "acp",
    "agent-client-protocol",
    "ai-agents",
    "wicked"
  ],
  "files": [
    "*.mjs",
    "README.md"
  ],
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
